# Used by every Dockerfile that builds from the repo root (admin-app/Dockerfile,
# and backend/Dockerfile — the latter also builds chat-app's SPA into itself,
# so backend/ can't be blanket-excluded here the way it used to be).
node_modules/
*/node_modules/
.git/
.claude/
**/dist/
**/.venv/
backend/__pycache__/
backend/.pytest_cache/
backend/.ruff_cache/
backend/credentials/
backend/uploads/
.env
.env.*.local
