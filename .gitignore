# Node
node_modules/
dist/
dist-ssr/
*.local
*.tsbuildinfo
# tsc -b occasionally emits compiled output next to a .ts config file if a
# tsconfig's noEmit gets misconfigured — these should never be committed.
tailwind.config.js
tailwind.config.d.ts
vite.config.js
vite.config.d.ts

# Python
backend/.venv/
backend/__pycache__/
**/__pycache__/
*.pyc
backend/.pytest_cache/
backend/.ruff_cache/
backend/*.egg-info/

# Env
.env
.env.*.local
!.env.example

# Editors / OS
.DS_Store
.vscode/*
!.vscode/extensions.json
.claude/

# Logs
*.log
npm-debug.log*

# Docker
postgres-data/

# Uploaded astrologer attachments (local disk storage, dev-only)
backend/uploads/

# Google service-account credentials (Sheets sync) — never committed
backend/credentials/
