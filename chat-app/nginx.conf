server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Client-side routed SPA — same reasoning as admin-app/nginx.conf.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
